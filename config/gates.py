"""Buy-gate thresholds — imported from settings for convenience."""

from config.settings import settings

MIN_FINAL_SCORE = settings.min_final_score
MIN_REWARD_RISK = settings.min_reward_risk
MIN_DOLLAR_VOLUME = settings.min_dollar_volume
MIN_RISK_SCORE = settings.min_risk_score
MIN_DS_SCORE = settings.min_ds_score
